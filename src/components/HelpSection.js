import React from 'react';

const HelpSection = () => (
  <div style={{ marginBottom: '20px', padding: '10px', backgroundColor: '#f0f0f0', borderRadius: '5px' }}>
    <p>This tool visualizes CUENDE traffic data. Upload a GeoJSON file to see the road network. Click on roads for details. Use the controls to navigate between connected road segments. The right panel lists all roads in the dataset.</p>
  </div>
);

export default HelpSection;